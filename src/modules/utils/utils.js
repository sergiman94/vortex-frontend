const apiTimeout = 60000

const API_URL = "http://localhost:3000" 

const API_CONNECTIONS = "http://localhost:3000/v1/connections"

module.exports = {
    apiTimeout,
    API_URL,
    API_CONNECTIONS
}